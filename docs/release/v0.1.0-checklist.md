# SkillCopilot v0.1.0 Release Checklist

Status: **Release Candidate — ready with manual gates**
Date prepared: 2026-07-23
Feature baseline commit: `5058e06f4409cf22f8a0c9ae329f25dba4ef2997`
RC session also includes a small hooks-order fix in Skills/Agents empty states (must be in the RC commit).

Legend:

- `[x]` verified in this session with evidence
- `[ ]` 待人工验证 / not verified here
- `[~]` verified at process level only (not pixel / not native dialog)

## Code and configuration

- [x] `package.json` / `Cargo.toml` / `tauri.conf.json` version = `0.1.0`
- [x] Identifier `io.github.agdh.skillcopilot`
- [x] productName / window title `SkillCopilot`
- [x] Bundle active with Windows icons present and readable
- [x] Capability includes `dialog:allow-open` (plus core/opener defaults); not widened
- [x] No runtime hardcoded `E:\SkillCopilot` / `WORKSPACE_ROOT_PATH` under `src`
- [x] No SQLite crate / npm package
- [x] No tracked secrets / real `.env`
- [x] CSP left as `null` (unchanged; documented, not blindly hardened)
- [x] Icons readable (`icon.ico` / PNGs / `icon.icns`). Scaffold-era assets; not redesigned.

## Automated / agent verification

- [x] `pnpm install --frozen-lockfile` → exit 0
- [x] `pnpm build` → exit 0 (re-run after hooks fix → exit 0)
- [x] `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` → exit 0
- [x] `cargo test --manifest-path src-tauri/Cargo.toml` → **70 passed**
- [x] `cargo check --manifest-path src-tauri/Cargo.toml` → exit 0
- [x] `pnpm tauri info` → exit 0
- [x] `git diff --check` → exit 0 (after docs edits; re-check before commit)
- [x] Browser regression (out-of-repo Playwright / `pnpm dev`)
- [~] `pnpm tauri dev`: Vite ready, `Finished dev`, `skillcopilot.exe` launched; no panic in log. Native dialog not automated.
- [x] `pnpm tauri build` → exit 0; MSI + NSIS produced
- [~] Release exe `Start-Process` smoke: process stayed alive ~6s, WebView2 processes present; then terminated. No pixel QA.

## Product behavior (agent-assisted)

- [x] Trilingual Workspace picker strings present
- [x] Single global `role="status"` in `src/App.tsx`
- [x] Unselected Workspace empty states (browser)
- [x] Browser folder-pick failure toast
- [x] localStorage path restore (browser)
- [x] Locale switch keeps Workspace path (browser)
- [x] Forget Workspace clears preference when clear succeeds (browser)
- [x] Fixed React hooks-order crash when opening Skills/Agents without a Workspace
- [ ] Native Dialog cancel keeps Workspace
- [ ] Native Dialog select `E:\SkillCopilot` loads real Dashboard / Skills / Agents
- [ ] App restart restores Workspace + locale
- [ ] Settings forget then restart stays unset
- [ ] Skills/Agents search + dual-pane scroll in native WebView
- [ ] System clipboard copy in native WebView

## Packaging metadata (this host build)

| Artifact | Absolute path | Size (bytes) | SHA-256 | Authenticode |
| --- | --- | --- | --- | --- |
| Release exe | `C:\Users\asus\AppData\Local\Temp\cursor-sandbox-cache\70f41e4c85b7a978a5399ca42448811e\cargo-target\release\skillcopilot.exe` | 9650176 | `AD0600343546768C53D63F29E890C8CA08BB55411FD60419897ED759E859C89A` | NotSigned |
| MSI | `...\release\bundle\msi\SkillCopilot_0.1.0_x64_en-US.msi` | 3211264 | `8FBBCA55F77EB529C63BFA25B7B7FBB75FE74C76800F5D29CCF0C27514784BA2` | NotSigned |
| NSIS | `...\release\bundle\nsis\SkillCopilot_0.1.0_x64-setup.exe` | 2108491 | `41C824B10C77530E277CAF1752DBDD5E1ECFE2327A5C6C2872334F3078439D27` | NotSigned |

Note: this agent session’s Cargo target dir was redirected under the temp sandbox cache. Rebuild locally before publishing if you need artifacts under `src-tauri/target/release/`.

## Installer acceptance (manual — do not auto-run)

1. [ ] Double-click NSIS setup
2. [ ] Record SmartScreen / publisher status
3. [ ] Complete install
4. [ ] Launch from Start Menu
5. [ ] Select `E:\SkillCopilot`
6. [ ] Verify Dashboard, Skills count on this machine, 3 Agents
7. [ ] Close + restart; Workspace and locale restore
8. [ ] Settings → Forget Workspace
9. [ ] Uninstall; note program-file removal and whether WebView localStorage preference remains
10. [ ] Repeat with MSI on a clean state (avoid overlapping installer confusion)

## Publishing gates (after manual install acceptance)

- [ ] Create annotated tag `v0.1.0`
- [ ] Create GitHub Release
- [ ] Attach MSI + NSIS
- [ ] Paste SHA-256 into Release body
- [ ] Update CHANGELOG from `Unreleased` to dated `0.1.0`
- [ ] Final human sign-off

## Notes

- Non-zero exits from intentionally stopped `pnpm dev` / `pnpm tauri dev` are cleanup, not build failures.
- Do not claim pixel-level native WebView QA unless interactive evidence exists.
